#!/usr/bin/env node
// Requires `npm run build` first. For development, use `npm run cli -- <args>` instead,
// which runs src/cli/index.ts directly via tsx without a build step.
import "../dist/cli/index.js";
