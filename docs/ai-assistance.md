# AI Collaboration Log

Methodology record for human–AI development on SpecTrace
(build plan §0.2). One line per delegated task.

| Date | REQ / Task | Surface | Extent |
|---|---|---|---|
| 2026-08-01 | Specs: SPEC-CLI-000, SPEC-APP-000, setup + build plans | Claude chat | pair-design; drafts reviewed & owned by BP |
| 2026-08-01 | Phase A scaffold (workspaces, CI, CLAUDE.md, package stubs) | Claude chat | scaffold |
| 2026-08-01 | Prelim harness (spec-trace-prelim repo: indexer, BM25F, metrics, CLI; built across prior sessions) | Claude Code | draft; reviewed by BP |
| 2026-08-01 | Experiment repo freeze record (unjs/hookable v6.1.1; §5.2 record; tag→SHA verified) — selection by BP | Claude Code | scaffold |
| 2026-08-01 | Port prelim harness into monorepo as packages/prelim + fixtures (todo-example, experiment/) | Claude Code | scaffold |
| 2026-08-01 | REQ-CORE-010/011/020: promote indexer + BM25F retrieval into @spectrace/core with AC-mapped tests; prelim rewired to consume core | Claude Code | draft (promoted from reviewed spike code) |
| 2026-08-01 | HOOK-001..012 transcription: BP-authored requirement statements (strata assigned by BP) transcribed into §6.3 schema files; retrieval-relevant text (title/statement/ACs) preserved verbatim or decomposed from BP wording, no vocabulary added; rationale/notes (non-retrieval fields) drafted by Claude | Claude Code | scaffold/transcription |
| 2026-08-01 | Ground-truth links (fixtures/ground-truth/hookable.json, 24 links, validate: 0 errors): authored by BP with the help of a separate AI session; labels owned by BP | BP + Claude chat (separate session) | pair-labeling; BP-owned |
| 2026-08-01 | REQ-CORE-020: BM25F revisions bm25f-v2 (function-word stopwords, non-empty-field length normalization, file/module kind prior 0.5) and bm25f-v3 (plural folding) with tests; diagnosis from retrieval output + aggregate metrics only — ground truth never read (blinding wall §0.2); evaluation runs executed by BP | Claude Code | draft; reviewed & measured by BP |
| 2026-08-02 | Retire packages/prelim (logic already promoted to core; BP retains external copy); spec SPEC-CLI-000 v0.2.0: new §10 Evaluation (REQ-CORE-070/071) + REQ-CLI-009 `spectrace evaluate`, moving prelim-spec §10/§15 metric & run-record capability into core/CLI | Claude Code | proposed; pending BP review |
| 2026-08-02 | REQ-CORE-070/071 + REQ-CLI-009: metrics & run artifacts promoted from prelim into core with AC-mapped tests; CLI `index`/`analyze` (lexical subset)/`evaluate retrieval` commands + interim §6.3 requirement parser; help test updated to nine commands (snapshot change authorized by BP); synthetic test fixtures only — ground truth never read (blinding wall §0.2) | Claude Code | draft |
| 2026-08-02 | Phase A descope (decision by BP): LLM ranking (PQ3 + PQ4 cost portion) deferred to Phase D, controlled drift (PQ5) to Phase F; build-plan Phases A/D/F and prelim-spec DoD/PQs/checklists amended; deferrals to be stated in feasibility-report limitations | Claude Code | scaffold (edits); decision & review BP |
| 2026-08-02 | REQ-CORE-020: bm25f-v4 (corpus-derived identifier protection for v2 stopwords; plural-folder fixes: sses sibilant class, length floor 2) — synthesized from a 4-lane Opus diagnosis fan-out over retrieval output, term space, and score composition; evaluation runs executed by Claude per amended rule 1 (path passed to CLI, aggregate metrics only read, ground truth never opened) | Claude Code + 4 Opus subagents | draft; diagnosis/impl delegated, diff reviewed by orchestrator; pending BP review |
| 2026-08-02 | REQ-CORE-020: bm25f-v5 (containment-derived aggregate prior, α=0.15 — generalizes v2's file/module kind prior to container classes via line-span containment; leaf-order invariant) from the same pass-1 diagnosis; evaluation by Claude per amended rule 1, aggregates only | Claude Code + 1 Opus subagent | draft; impl delegated, diff reviewed by orchestrator; pending BP review |
| 2026-08-02 | REQ-CORE-020: bm25f-v6 candidate (normalizedSource TF saturation κ=2) implemented, measured, REGRESSED vs v5 (overall R@3 .708→.625, R@5 .750→.667) and reverted per loop protocol; negative result retained as patch + run artifact (runs/opt/results-v6.jsonl) for the feasibility report's error analysis; evaluation by Claude per amended rule 1, aggregates only | Claude Code + 1 Opus subagent | negative result; reverted, not merged |
| 2026-08-02 | Parked-proposal dispositions (BP delegated the calls): near-tie diversity rerank implemented as bm25f-v7 candidate with spec sentence, measured, REGRESSED (partial-overlap R@3/@5 .750→.500 — near-tied same-file siblings are often multiple labeled symbols), reverted including spec sentence, patch retained; README-section doc-attachment deferred to Phase C as build-plan candidate (needs REQ-CORE-010 spec change; validity caveat: requirements authored from that README); k1 retune rejected (same family as reverted v6 saturation). Overfitting cap (4 measured versions beyond v3) reached — bm25f-v5 stands as default; evaluation by Claude per amended rule 1, aggregates only | Claude Code | decisions by Claude under BP delegation; pending BP review |
| 2026-08-02 | bm25f-v4/v5 aggregate metrics independently reproduced by BP with his own analyze/evaluate runs; v5 confirmed as default. Claude-run evaluations for the optimization loop are thereby verified; report may cite the v5 numbers | BP | verification; BP-owned |
| 2026-08-02 | docs/feasibility-error-analysis.md drafted from blinding-safe evidence only (aggregates + label-free diagnosis; ground truth never opened): failure mechanisms M1–M5, §14 taxonomy mapping, negative-result record, next-step recommendations, threats-to-validity notes; per-miss classification table left for BP (ground-truth required) | Claude Code | draft; §3 completion + ownership BP |
| 2026-08-02 | Phase A gate closed by BP: GO on retrieval quality (bm25f-v5, BP-verified byte-identical artifacts); gate-closure note added to build plan; carried items: §14 per-miss classification (BP), deferred PQ3/PQ4/PQ5 (Phases D/F) | BP + Claude Code | decision BP; recording Claude |
| 2026-08-02 | Phase B schema freeze, step 1 — four gating decisions made by BP (config format YAML; one-REQ-per-file granularity; relax REQ-CORE-001 rather than re-freeze the experiment corpus; core-spec split before implementation), then executed: SPEC-CORE-000 extracted from SPEC-CLI-000 v0.2.0 per its §14 (IDs unchanged), 35 requirement bodies moved to specs/requirements/, CLI spec rewritten to v0.3.0 as command-surface-only with a depends-on pin, cross-references repointed. REQ-CLI acceptance criteria decomposed verbatim from existing statement clauses (same transcription convention as the HOOK fixtures — no intent added); REQ-CORE-011's compound AC split; `partial` status value introduced. Verified: all 35 files parse and satisfy the relaxed schema, all 12 frozen HOOK fixtures validate unmodified with the freeze hash intact | Claude Code | draft; decisions BP, pending BP review |
| 2026-08-02 | Phase B gate closed by BP: all three criteria met — `init`/`validate` verified by Claude (55 requirements, 0 violations, frozen corpus intact), and BP ran Studio, opened the vault, and confirmed tree and markdown previews render. `partial` retained as a status value after its trial. Gate-closure note added to the build plan with carried-forward items (Studio editor, JSON envelopes to core, Appendix A repointing) | BP + Claude Code | decision & verification BP; recording Claude |
| 2026-08-02 | Studio `pnpm dev` failure (repo tooling, no REQ) — BP hit `Error: Electron uninstall`: the electron package was present but its runtime binary was not, and pnpm had the postinstall recorded as complete, so neither `pnpm install` nor `pnpm rebuild electron` would retry it (likely an incomplete postinstall during the original add, which logged registry EPERM retries). Fixed by running electron's `install.js` directly and adding an explicit `pnpm.onlyBuiltDependencies` allow-list to the root package.json for electron and esbuild — which also cleared a standing "Ignored build scripts: esbuild" warning. Verified: binary reports v43.2.0 and `pnpm --filter @spectrace/studio dev` now reaches "starting electron app". Recovery command documented in CLAUDE.md. My miss — I added Electron without ever checking its binary landed | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-APP vault extraction (decision by BP): all 20 REQ-APP requirements moved from specs/spectrace-product-spec.md into specs/requirements/, one file per requirement, matching the REQ-CORE/REQ-CLI treatment; product spec §4 rewritten as narrative with generated tables (v0.3.0), NFR-APP left inline for want of acceptance criteria; `spec:index` extended to the product spec. Vault grew 35 → 55 requirements, all validating clean. Status calls: REQ-APP-001 → `partial` (tree display holds, with a new test covering the stated 1,000-file/2 s bound; file operations and external-change detection do not), REQ-APP-002 stays `proposed` — Studio renders GFM but read-only in a separate pane, and AC2 asks for rendering *in live preview*, which the requirement defines as editable-in-place | Claude Code | draft; decision BP, pending BP review |
| 2026-08-02 | Studio walking skeleton (setup plan Phase 3; REQ-APP-001/002 partial): `apps/studio` scaffolded by hand rather than via `pnpm create @quick-start/electron`, which is interactive and would have dumped unreviewed files — electron-vite + React 19 + TS, with main/preload/renderer/shared layout and the typed `Api` IPC contract from setup plan §3.2. Vault reading (`src/main/vault.ts`) is deliberately Electron-free so it unit-tests in plain Node: recursive markdown tree, deterministic ordering, vendored/VCS directories excluded, POSIX paths, and path-escape rejection on reads. Renderer is the full walking skeleton — pick folder → tree → click file → markdown renders via react-markdown + remark-gfm — read-only, with contextIsolation on and a renderer CSP. 10 tests. Peer-dependency pinning needed: vite 7 (electron-vite 5 rejects 8), @vitejs/plugin-react 5, and TypeScript held at 5.6 to match the monorepo rather than the 7.x that resolved by default. CI gains ELECTRON_SKIP_BINARY_DOWNLOAD. REQ-APP status flips deferred pending the vault-extraction decision | Claude Code | draft; pending BP review |
| 2026-08-02 | Phase B gate check (unit 6, loop-driven): `spectrace init` scaffolded `.spectrace/` on this repository (6 files, exit 0), a second run changed nothing (exit 0), and `spectrace validate` reported 35 requirements with 0 violations and 0 warnings (exit 0). Frozen HOOK corpus re-validated at 12/12 clean with all 12 SHA-256 hashes intact. Full sweep green: `--frozen-lockfile`, build, typecheck, 177 tests, `spec:index:check`. Repo policy decided by BP: `.spectrace/config.yaml` and templates are committed (shared per-repo settings, so CI validates against the same config), generated index artifacts gitignored per REQ-CORE-012. Two of the three gate criteria are met; "Studio opens the vault read-only" remains outstanding because Studio Phase 3 was held — closing the gate is BP's call | Claude Code | draft; gate decision BP |
| 2026-08-02 | REQ-CLI-001 (Phase B unit 5, loop-driven): `spectrace init` scaffolds `.spectrace/config.yaml` plus a templates directory holding one file per REQ-CORE-003 template kind. Content comes from core — `renderDefaultConfig()` added to the config module, since REQ-CORE-004 owns the format and Studio's settings surfaces will want the same serialization — and the CLI only decides where bytes land. Idempotent by default, `--force` to overwrite, and `--force` touches only the files it manages. 9 CLI tests plus 2 core tests asserting the rendered default config round-trips to DEFAULT_CONFIG exactly, so an initialized repo behaves identically to a bare one. One test validates the scaffolded templates through the real CLI, closing REQ-CORE-003 AC1 end to end. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CLI-002 (Phase B unit 4, loop-driven): `spectrace validate` over the configured specification paths, wiring REQ-CORE-004's config loader to REQ-CORE-001/002's validator; documents re-rooted on their configured path so violation messages name a findable file; versioned `spectrace.validation-report` JSON envelope; exit 3 on violations, 0 clean, 1 on a missing repository. 9 tests including an explicit check that a broken document *outside* every configured path is not validated (AC1's "exactly"). Dogfooded: `pnpm cli validate --repo .` reports 35 requirements, 0 violations, exit 0. CLI spec §3 "currently shipping" note corrected from six stubs to five. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CORE-003 (Phase B unit 3, loop-driven): five specification templates (use case, functional, non-functional, architecturally significant, acceptance criteria) in `packages/core/src/templates/`, embedded as strings because tsc emits only JS and a `.md` under src/ would never reach dist/. `nextRequirementId` generates a collision-free ID from the IDs already in the vault, widening the zero-padding if the vault already uses wider numbers. 19 tests; each template is validated unedited through the REQ-CORE-001/002 validator, individually and as a set. Interpretation flagged for BP: the "acceptance criteria" template is rendered as a full requirement document rather than a paste-in fragment, since AC1 requires every template to instantiate with an ID and pass validation — the fragment reading contradicts its own AC. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CORE-004 (Phase B unit 2, loop-driven): YAML configuration loader in `packages/core/src/config/` — `.spectrace/config.yaml` with a `version` field covering spec paths, exclusions, retrieval mode and k, model/embedding settings, and confidence bands; every field resolves to a documented default so callers never branch on absence. Missing file, malformed YAML, unknown keys (reported by dotted path), and out-of-range values are all warnings, never failures. `toPlainValue` extracted to `src/internal/` and shared with the schema parser so both normalize at the YAML boundary. `RetrievalMode`/`ConfidenceBands` moved out of the index.ts scaffold into the config module. 17 tests including two source-greps asserting the engine reads no env vars and writes no console output, with a guard test so those greps cannot pass vacuously. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CORE-001 + REQ-CORE-002 (Phase B unit 1, loop-driven): requirement schema parsing and set validation in `packages/core/src/schema/` — frontmatter parse with structuredClone-safe normalization, mandatory id/title/status, priority default P1, rationale from frontmatter or `## Rationale` body section, trace-link record shape, acceptance criteria from frontmatter or body, duplicate-ID detection naming every colliding file, all violations reported in one pass. 22 AC-mapped tests. Phase A stub types in `index.ts` (capitalized status vocabulary, mandatory rationale/priority) reconciled to the amended schema and the `vault` stub replaced. Dogfooded: this repo's 35 requirement files and the 12 frozen HOOK fixtures both validate with 0 violations and 0 warnings, all 12 fixture rationales resolving from body sections. Status flips: both → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | Single-source-of-truth fix (repo tooling, no REQ) — BP identified that the extracted narrative specs duplicated title/priority/status from the requirement frontmatter, i.e. two sources of truth for the same fields. Resolved by generation rather than deletion: `scripts/spec-index.mjs` rebuilds each marked table from `specs/requirements/` frontmatter, `pnpm spec:index:check` added to CI as the staleness guard. Also recorded: ground truth is frozen permanently (Phase B milestone row discharged, not pending); `partial` adopted on trial, revisit at the Phase B gate; validation walks `specs/requirements/` only — the narrative documents are prose, not requirement documents. Verified both directions: a hand-edited status fails the check (exit 1), and a new requirement file joins its section with no narrative edit | Claude Code | draft; problem spotted by BP, decisions BP |
| 2026-08-02 | CI health (SPEC-CLI-000 Phase A scaffold, no REQ): cross-OS typecheck failure diagnosed — `pnpm typecheck` ran before `pnpm build`, so `@spectrace/cli` could not resolve `@spectrace/core` (its `types` points at `dist/index.d.ts`, build-only); the three trailing TS18046/TS7006 errors were cascades of that one unresolved import, reproduced locally by moving `packages/core/dist` aside. Steps reordered build→typecheck (merged by BP as 0b7597f). Follow-up: GitHub Actions bumped to node24 runtimes (checkout v4→v7, pnpm/action-setup v4→v6, setup-node v4→v7), clearing the Node 20 deprecation warning; breaking changes checked as inapplicable and tags verified via git ls-remote | Claude Code | draft; pending BP review |
| 2026-08-02 | Root README rewritten for public presentation (repo polish, no REQ — Phase H item pulled forward at BP's request): problem statement, pipeline diagram, phase/gate table, per-family requirement progress, command-surface table, layout, dev commands, research context including the blinding wall and this log. Facts sourced from the build plan's gate-closure notes, requirement frontmatter, and a local `pnpm test` run (188); CI badge wired to the ci.yml workflow. License section deliberately left as "not yet licensed" per BP | Claude Code | draft; pending BP review |
| 2026-08-03 | REQ-CORE-012 + REQ-CLI-003 (Phase C unit 1, loop-driven): local index artifact and `spectrace index --rebuild`. Format is JSONL owned by core (`indexer/index-artifact.ts`) — a header naming the artifact, version, and the inputs that produced it, then one symbol per line — matching the REQ-CORE-071 run-artifact shape. Byte-identity (AC1) is protected beyond the indexer's existing sort by an explicit field projection, so a refactor that reorders an object literal cannot change the layout, and by carrying no timestamp or environment-derived field. `--rebuild` discards and re-indexes (REQ-CLI-003 AC2); the default path reuses an index whose commit, engine version, and exclusion set still match, on a clean tree only, with the index file itself discounted from the dirtiness check. Per-file incremental scoping deliberately left to REQ-CORE-060 (Phase F). Exclusions now come from `.spectrace/config.yaml` with `--exclude` adding to them (REQ-CORE-011). 16 core + 9 CLI tests. Dogfooding this repo surfaced a pre-existing bug in the counts-by-kind map: `constructor` is a real symbol kind, so `counts["constructor"] ?? 0` was resolving to `Object.prototype.constructor` and printing a stringified function — fixed with a null-prototype map, regression test added. Status flips: REQ-CORE-012 → `implemented`, REQ-CLI-003 `partial` → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-03 | REQ-CORE-023 (Phase C unit 2, loop-driven): bounded candidate sets — the single gate every model payload passes through (`packages/core/src/transmission/bounded-payload.ts`). Built so a wider payload is not expressible rather than merely discouraged: the module never receives a repository path (so excerpt text can only come from already-indexed symbols), it resolves candidates *through* each requirement's retrieved set rather than through the symbol table (so an unretrieved symbol cannot enter a payload), and every field is length-budgeted (so size scales with requirements × k, not with one large file). `auditTransmissionLog` re-derives the permitted excerpt count and reports excess in five rules, making AC1 checkable after the fact rather than only by inspection; a log that omits a requirement is reported incomplete but still `bounded`, since boundedness is about excess only. Surfaced as `spectrace analyze --dry-run [--transmission-log <file>]`, which has no code path to a model or embedding call; `--req <id>` also landed. 15 core + 2 CLI tests. Dogfooded on the frozen experiment corpus: 12 requirements × k=10 = exactly 120 excerpts, bounded, 128 KB — ground truth not involved. Status flips: REQ-CORE-023 → `implemented`; REQ-CLI-004 `proposed` → `partial` (AC1 holds, AC3 half — estimated cost waits on REQ-CORE-032) | Claude Code | draft; pending BP review |
| 2026-08-03 | Phase C open items resolved (decisions by BP, recorded by Claude): Configuration B embeds with OpenAI `text-embedding-3` (model identifier stays configuration, so this sets the evaluation baseline rather than constraining the engine; the key is injected by the client since core reads no env vars); REQ-CORE-022's undecided merge strategy resolved as "implement both, measure, then choose" — `rrf-v1` and `weighted-v1` behind one versioned registry, default picked from the frozen-corpus numbers. Noted against `weighted-v1`: its α counts against the same overfitting budget the Phase A measured-version cap protects | Claude Code | recording; decisions BP |
| 2026-08-03 | REQ-CORE-021 (Phase C unit 3, loop-driven): semantic retrieval, Configuration B. Core declares an `EmbeddingProvider` interface and the client supplies one — the no-env-vars rule forces this, and it has the useful side effect of making AC1 testable without a network, since a provider that counts its own calls proves "zero API calls on the second run" directly. Cache entries are keyed by a hash of the *embedded text*, not by symbol ID: a symbol keeps its ID across edits to its body, so a symbol-keyed cache would serve a vector for text that no longer exists — text-keying makes per-symbol content invalidation fall out automatically, dedupes identical text, and puts the model ID in the header so a cache written by another model is discarded rather than merged. Untouched entries are pruned per run (a bug caught by its own test: `touched` initially accumulated across runs sharing a cache object, so `beginRun()` now scopes it). Embedded text draws on the same fields BM25F does so A and B are compared on equal information. Vendor adapter for OpenAI `text-embedding-3` lives in the CLI (`embedding-provider.ts`) with retry on 429/5xx, no retry on 4xx, and response items ordered by their `index` rather than array position, since core matches vectors to inputs positionally. 24 core + 15 CLI tests, none touching the network. Status flip: REQ-CORE-021 → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-03 | REQ-CORE-022 (Phase C unit 4, loop-driven): hybrid retrieval, Configuration C — both strategies per BP's resolution. `rrf-v1` merges on ranks, so it needs no calibration between unbounded BM25 scores and cosine similarities bounded to [−1,1]; `weighted-v1` min-max normalizes per requirement then blends with α, treating absence from a list as no contribution rather than a zero score. Both carry a configuration identifier naming strategy, parameter, and both inputs, so a run stays attributable. Two design points worth recording: the candidate pool is deliberately 2k wide and truncated to k *after* merging (a merge of two k-truncated lists can only see k per list, and disagreement is the whole premise), and selection is by `.spectrace/config.yaml` `retrieval.mode`/`topK` with every flag an override — which is what AC1's "purely by configuration" actually demands. A test fixture bug caught by its own assertion: the rrfK-sensitivity test used a symmetric fixture where the top two tied at every k, measuring nothing; replaced with two agreeing lists. 27 core + 2 CLI tests. Status: REQ-CORE-022 → `partial`, not `implemented` — merge strategies and config-driven selection hold, but AC1's second clause (harness runs all three against the same index) is a capability not yet demonstrated end to end, since B and C need a live embedding key. `rrf-v1` is the provisional default; the real default is BP's call after the frozen-corpus comparison | Claude Code | draft; pending BP review |
| 2026-08-03 | Phase C unit 5 (evaluation harness, loop-driven): `evaluate sweep` runs several configurations against one index, evaluates each, and writes results/metrics/comparison artifacts — turning BP's seven-command evaluation into one; `evaluate compare` aligns already-computed metrics artifacts into report-ready Markdown/CSV/terminal tables. Core owns the alignment (`evaluation/comparison.ts`), clients own the rendering, per the no-formatting-in-core rule. Two things it refuses to do quietly: reports computed over different k values or strata are aligned to the intersection but the comparison carries an `omitted` list that every renderer reproduces (a table that silently drops a stratum reads as "we measured everything"), and a sweep where one configuration fails reports the skip and exits 1 rather than presenting a subset as success. The retrieval dispatch was extracted to `cli/src/retrieval-run.ts` and is now shared by `analyze` and `sweep`, so a sweep cannot report numbers that describe something other than the shipped command. 14 core + 9 CLI tests. Dogfooded on the frozen corpus (Configuration A): overall R@5 .750, R@10 .917, per-stratum rows intact — byte-for-byte the recorded bm25f-v5 Phase A figures, confirming the refactor moved nothing; aggregates only, ground truth never opened. **Flagged for BP: `evaluate sweep` collides with REQ-CLI-009 AC2 ("requires no network access") in semantic/hybrid mode.** The statement scopes its clauses to `evaluate retrieval` and metric computation stays network-free, so the recommendation is to scope AC2 explicitly and name the subcommands; ACs left unedited pending that call | Claude Code | draft; spec amendment proposed, pending BP decision |
| 2026-08-03 | Phase C unit 5b (chart output, loop-driven): Recall@k figures as self-contained SVG (`cli/src/comparison-chart.ts`) — horizontal grouped bars, one group per stratum, one bar per configuration; `evaluate sweep` writes one figure per k, `evaluate compare --chart` writes one on demand. Followed the dataviz skill's procedure rather than picking colours by eye: form first (categorical identity → grouped bars; horizontal because stratum labels are long), then the reference palette's slots 1–3, then **ran the validator** — light and dark both PASS all-pairs (worst CVD ΔE 9.2 light / 9.4 dark, normal-vision 24.0 / 20.9). Light-mode aqua measures 2.74:1 against the light surface, below the 3:1 bar, so the relief rule applies and is discharged twice: a visible value label on every bar, plus the Markdown/CSV table twin. Three configurations is a hard cap, not a coincidence — a fourth is dropped with a stated footnote rather than given a generated hue. Figures reproduce the comparison's `omitted` list as footnotes, matching the tables. Reading the generated output caught three defects the validator cannot see: float noise in coordinates (`681.6666666666666`), 26px of dead air reserved for a legend that a single-series chart does not draw, and a subtitle indented to the axis that overflowed the canvas; a fourth — footnotes colliding with the axis title — came from checking the vertical budget by hand. Browser screenshotting was declined, so layout is asserted numerically instead (every coordinate inside the canvas, bar pitch = 14+2 gap, no coordinate past two decimals, footnotes clear of the axis band). 18 CLI tests, three of which initially failed on my own bad assertions (`xmlns` is not a network fetch; the legend class appears in the stylesheet regardless; `0.780` is a value label, not a coordinate) | Claude Code | draft; pending BP review — figures not yet eyeballed by a human |
| 2026-08-03 | Phase C unit 6 (Studio editor track, loop-driven): REQ-APP-002/003/004 surfaces on top of core. CodeMirror 6 replaces the read-only react-markdown pane; live preview is implemented as **decorations over the source**, not a rendered model serialized back — which makes REQ-APP-002 AC1 (round-trip produces no diff) true by construction rather than by testing, since there is only ever one representation of the document. Syntax marks hide only while the caret is outside their construct, so the markdown stays reachable. New IPC: `writeFile` (ordinary `writeFileSync`, confined to the vault root and to `.md`) and `analyzeVault`, which validates the whole vault through `@spectrace/core` and builds the `[[wiki-link]]` graph in one pass. The load-bearing design point for the gate: analysis takes an **unsaved-buffer override**, so validation describes what is on screen rather than the last save — without it a duplicate ID typed but not saved would not surface at all. Wiki-link resolution falls back to requirement ID last, which matters because REQ-CORE-001 AC3 makes IDs independent of file names. 21 new Studio tests (32 total). Status flips, all deliberately conservative: REQ-APP-002 → `partial` (AC1/AC3 hold; tables and fenced code do not render in place), REQ-APP-003 → `partial` (AC3 holds for in-app edits only — external edits need the REQ-APP-001 AC3 watcher; no autocomplete, no rename), REQ-APP-004 → `partial` (**AC2 holds — this is the gate criterion**; no create-from-template, trace links not yet chips), REQ-APP-001 stays `partial` (AC2 holds for save; create/rename/move/delete and the watcher absent). **Not verified by a human: I could not launch the Electron app or screenshot it — build, typecheck, and tests pass, but nobody has seen this run.** BP must run `pnpm --filter @spectrace/studio dev` and confirm the gate criterion by eye | Claude Code | draft; pending BP review and a human run of the app |
| 2026-08-04 | Phase C evaluation analysed; REQ-CORE-022 merge default decided (BP delegated the call). BP ran A/B/C on the frozen corpus; the `weighted-v1` arm was missing, so it was added — which surfaced a flaw in my own CLI: a fully cached run performs zero API calls yet still demanded a key, contradicting REQ-CORE-021 AC1 and blocking offline reproduction of a recorded run. Fixed with a cache-only provider built from the cache header's model ID and dimensions, which refuses to embed anything new; the weighted arm then ran at zero cost and zero calls. That also turns AC1 from an assertion into an observation — the run either succeeds without a key, proving every vector was cached, or fails naming how many texts were missing. 6 new CLI tests. **Decision: `rrf-v1` stays the default** (MRR .794 vs .750, R@1 .625 vs .583, tied at R@5/R@10, trails only at R@3) — but recorded explicitly as parsimony, not evidence: the spread is at most 0.042 overall, which at n=12 is half a requirement, i.e. noise, and `rrf-v1` carries no tunable α to justify. **Two findings that matter more than the merge choice:** semantic beats lexical by a wide, consistent margin (overall R@5 .750 → 1.000, MRR .515 → .774, same direction at every k and stratum — 3 requirements, a real result), and **hybrid does not beat semantic** (R@5 .875 vs 1.000: RRF gives each list an equal vote and the lists are not of equal quality, so merging in lexical costs a requirement semantic alone retrieved). Changing the default `retrieval.mode` is a cost decision as well as a quality one and is left to BP. Blinding wall observed: only aggregate comparison output read; `metrics-*.json` deliberately not opened, since it carries per-requirement `requirementsWithoutGroundTruth`. REQ-CORE-022 → `implemented` | Claude Code | decision delegated by BP; analysis and fix by Claude, pending BP review |
| 2026-08-04 | REQ-CORE-023 AC2 (transmission-log under-reporting, fixed at BP's instruction from `docs/phase-c-open-decisions.md` §1): `analyze --dry-run --transmission-log` reported the bounded payload and said nothing about the fact that semantic and hybrid retrieval had already embedded **every symbol in the repository** — a log that satisfied AC1 while leaving a reader with a materially false picture of the run, which is precisely what NFR-CORE-005 forbids. The log now carries a mandatory `retrieval` section (artifact version 1 → 2, since a v1 log cannot describe Configuration B or C honestly): the mode, and where a model was involved, its identity, dimensions, the corpus-wide symbol and query text counts, and the split between texts sent over the network and texts served from the local cache. `auditTransmissionLog` gained three rules — `undisclosed-embedding` (a transmitting mode that says nothing), `unexpected-embedding` (a lexical log claiming a transmission it cannot have made), `mode-mismatch` (the log describing a different run than the one that happened) — and a `disclosed` result **deliberately independent of `bounded`**, because the defect was a perfectly bounded payload alongside total silence about the corpus; collapsing the two would have hidden exactly the case that motivated the fix. The model is named even on a fully cached run: those vectors came from that model on whichever earlier run paid for them, and omitting it would leave a reader unable to say where the corpus went. Two smaller lies fixed alongside: the JSON report hard-coded `embeddingCalls: 0` (now a counted `embeddedTextCount`), and `--dry-run`'s own help text promised "zero model or embedding calls" while the semantic path embedded the corpus before the dry run began (now states what it does — no ranking-model call, retrieval still runs). 10 core + 4 CLI tests. **Not changed, and flagged:** `--dry-run` still permits retrieval to reach the network in semantic/hybrid mode. Making it imply cache-only would alter the runtime contract, not just the report, so it is BP's call — the honest report is the fix that was authorized. Status: REQ-CORE-023 stays `implemented`, now with AC2; the separate question of whether its *statement* permits corpus-wide embedding at all remains open as §2 of the Phase C decisions | Claude Code | draft; defect and fix by Claude at BP's instruction, pending BP review |
| 2026-08-04 | Phase C closed out — three decisions applied plus one delegated. **BP:** default retrieval mode stays Configuration A; REQ-CLI-009 AC2 amendment accepted as recommended (scoped to `evaluate retrieval` and metric computation, `compare`/`sweep` named in the statement); Studio and the SVG figures verified by eye, discharging both human gate criteria. **Delegated to Claude — REQ-CORE-023 scope, resolved as "a model" means any model, embedding models included.** Three grounds: BP's usage of "model" is LLM-general and never meant the §6 ranking stage, so the narrow reading was never the plain sense of the text; the requirement's own rationale ("cost proportional to requirements, not repository size") is violated by corpus-wide embedding whichever kind of model receives it, and a reading under which a requirement's stated rationale does not apply to the case at hand is motivated reasoning rather than interpretation; and NFR-CORE-005 is a *privacy* requirement, which does not turn on model architecture — 424 symbol texts reaching a third party is the same event embedded or completed. Consequence: Configuration B was in violation as written, not merely under-reported, so the statement now names B and C as an explicit exception with three conditions (never the default, disclosed before it happens, refused without explicit acceptance) and gains AC3. Implemented as `requiresCorpusTransmissionConsent` in core — a pure predicate, so the matrix is testable without a network and Studio cannot invent a different rule — with `--accept-corpus-transmission` on `analyze` and `evaluate sweep`, refusing with `corpus_transmission_not_accepted` (exit 2). **Two boundaries chosen deliberately:** a fully cached run is *not* gated (consent is about content leaving the machine, and gating a run that sends nothing trains an operator to wave the prompt through — it would also break the offline reproduction REQ-CORE-021 AC1 exists to guarantee), and the refusal quotes the real uncached count rather than an upper bound, since a consent prompt that overstates the ask is one nobody reads. A repo-level config key for standing consent was considered and rejected: a flag keeps consent visible in every recorded command line, which the evaluation record wants. §1's "operates locally" claim and NFR-CORE-005 qualified to match; keeping A as the default is what holds the exception narrow. 9 core + 5 CLI tests. A pre-existing fixture-ordering dependency in cli.test.ts surfaced while adding them (four tests shared a directory created by whichever ran first, so `vitest -t` filtering failed) and was fixed with a self-sufficient fixture helper | Claude Code | decisions BP except REQ-CORE-023 scope, delegated; drafting and implementation Claude, pending BP review |
