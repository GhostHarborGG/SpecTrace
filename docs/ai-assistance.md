# AI Collaboration Log

Methodology record for human–AI development on SpecTrace
(build plan §0.2). One line per delegated task.

| Date | REQ / Task | Surface | Extent |
|---|---|---|---|
| 2026-08-01 | Specs: SPEC-CLI-000, SPEC-APP-000, setup + build plans | Claude chat | pair-design; drafts reviewed & owned by BP |
| 2026-08-01 | Phase A scaffold (workspaces, CI, CLAUDE.md, package stubs) | Claude chat | scaffold |
| 2026-08-01 | Prelim harness (spec-trace-prelim repo: indexer, BM25F, metrics, CLI; built across prior sessions) | Claude Code | draft; reviewed by BP |
| 2026-08-01 | Experiment repo freeze record (unjs/hookable v6.1.1; §5.2 record; tag→SHA verified) — selection by BP | Claude Code | scaffold |
| 2026-08-01 | Port prelim harness into monorepo as packages/prelim + fixtures (todo-example, experiment/) | Claude Code | scaffold |
| 2026-08-01 | REQ-CORE-010/011/020: promote indexer + BM25F retrieval into @spectrace/core with AC-mapped tests; prelim rewired to consume core | Claude Code | draft (promoted from reviewed spike code) |
| 2026-08-01 | HOOK-001..012 transcription: BP-authored requirement statements (strata assigned by BP) transcribed into §6.3 schema files; retrieval-relevant text (title/statement/ACs) preserved verbatim or decomposed from BP wording, no vocabulary added; rationale/notes (non-retrieval fields) drafted by Claude | Claude Code | scaffold/transcription |
| 2026-08-01 | Ground-truth links (fixtures/ground-truth/hookable.json, 24 links, validate: 0 errors): authored by BP with the help of a separate AI session; labels owned by BP | BP + Claude chat (separate session) | pair-labeling; BP-owned |
| 2026-08-01 | REQ-CORE-020: BM25F revisions bm25f-v2 (function-word stopwords, non-empty-field length normalization, file/module kind prior 0.5) and bm25f-v3 (plural folding) with tests; diagnosis from retrieval output + aggregate metrics only — ground truth never read (blinding wall §0.2); evaluation runs executed by BP | Claude Code | draft; reviewed & measured by BP |
| 2026-08-02 | Retire packages/prelim (logic already promoted to core; BP retains external copy); spec SPEC-CLI-000 v0.2.0: new §10 Evaluation (REQ-CORE-070/071) + REQ-CLI-009 `spectrace evaluate`, moving prelim-spec §10/§15 metric & run-record capability into core/CLI | Claude Code | proposed; pending BP review |
| 2026-08-02 | REQ-CORE-070/071 + REQ-CLI-009: metrics & run artifacts promoted from prelim into core with AC-mapped tests; CLI `index`/`analyze` (lexical subset)/`evaluate retrieval` commands + interim §6.3 requirement parser; help test updated to nine commands (snapshot change authorized by BP); synthetic test fixtures only — ground truth never read (blinding wall §0.2) | Claude Code | draft |
| 2026-08-02 | Phase A descope (decision by BP): LLM ranking (PQ3 + PQ4 cost portion) deferred to Phase D, controlled drift (PQ5) to Phase F; build-plan Phases A/D/F and prelim-spec DoD/PQs/checklists amended; deferrals to be stated in feasibility-report limitations | Claude Code | scaffold (edits); decision & review BP |
| 2026-08-02 | REQ-CORE-020: bm25f-v4 (corpus-derived identifier protection for v2 stopwords; plural-folder fixes: sses sibilant class, length floor 2) — synthesized from a 4-lane Opus diagnosis fan-out over retrieval output, term space, and score composition; evaluation runs executed by Claude per amended rule 1 (path passed to CLI, aggregate metrics only read, ground truth never opened) | Claude Code + 4 Opus subagents | draft; diagnosis/impl delegated, diff reviewed by orchestrator; pending BP review |
| 2026-08-02 | REQ-CORE-020: bm25f-v5 (containment-derived aggregate prior, α=0.15 — generalizes v2's file/module kind prior to container classes via line-span containment; leaf-order invariant) from the same pass-1 diagnosis; evaluation by Claude per amended rule 1, aggregates only | Claude Code + 1 Opus subagent | draft; impl delegated, diff reviewed by orchestrator; pending BP review |
| 2026-08-02 | REQ-CORE-020: bm25f-v6 candidate (normalizedSource TF saturation κ=2) implemented, measured, REGRESSED vs v5 (overall R@3 .708→.625, R@5 .750→.667) and reverted per loop protocol; negative result retained as patch + run artifact (runs/opt/results-v6.jsonl) for the feasibility report's error analysis; evaluation by Claude per amended rule 1, aggregates only | Claude Code + 1 Opus subagent | negative result; reverted, not merged |
| 2026-08-02 | Parked-proposal dispositions (BP delegated the calls): near-tie diversity rerank implemented as bm25f-v7 candidate with spec sentence, measured, REGRESSED (partial-overlap R@3/@5 .750→.500 — near-tied same-file siblings are often multiple labeled symbols), reverted including spec sentence, patch retained; README-section doc-attachment deferred to Phase C as build-plan candidate (needs REQ-CORE-010 spec change; validity caveat: requirements authored from that README); k1 retune rejected (same family as reverted v6 saturation). Overfitting cap (4 measured versions beyond v3) reached — bm25f-v5 stands as default; evaluation by Claude per amended rule 1, aggregates only | Claude Code | decisions by Claude under BP delegation; pending BP review |
| 2026-08-02 | bm25f-v4/v5 aggregate metrics independently reproduced by BP with his own analyze/evaluate runs; v5 confirmed as default. Claude-run evaluations for the optimization loop are thereby verified; report may cite the v5 numbers | BP | verification; BP-owned |
| 2026-08-02 | docs/feasibility-error-analysis.md drafted from blinding-safe evidence only (aggregates + label-free diagnosis; ground truth never opened): failure mechanisms M1–M5, §14 taxonomy mapping, negative-result record, next-step recommendations, threats-to-validity notes; per-miss classification table left for BP (ground-truth required) | Claude Code | draft; §3 completion + ownership BP |
| 2026-08-02 | Phase A gate closed by BP: GO on retrieval quality (bm25f-v5, BP-verified byte-identical artifacts); gate-closure note added to build plan; carried items: §14 per-miss classification (BP), deferred PQ3/PQ4/PQ5 (Phases D/F) | BP + Claude Code | decision BP; recording Claude |
| 2026-08-02 | Phase B schema freeze, step 1 — four gating decisions made by BP (config format YAML; one-REQ-per-file granularity; relax REQ-CORE-001 rather than re-freeze the experiment corpus; core-spec split before implementation), then executed: SPEC-CORE-000 extracted from SPEC-CLI-000 v0.2.0 per its §14 (IDs unchanged), 35 requirement bodies moved to specs/requirements/, CLI spec rewritten to v0.3.0 as command-surface-only with a depends-on pin, cross-references repointed. REQ-CLI acceptance criteria decomposed verbatim from existing statement clauses (same transcription convention as the HOOK fixtures — no intent added); REQ-CORE-011's compound AC split; `partial` status value introduced. Verified: all 35 files parse and satisfy the relaxed schema, all 12 frozen HOOK fixtures validate unmodified with the freeze hash intact | Claude Code | draft; decisions BP, pending BP review |
| 2026-08-02 | Studio `pnpm dev` failure (repo tooling, no REQ) — BP hit `Error: Electron uninstall`: the electron package was present but its runtime binary was not, and pnpm had the postinstall recorded as complete, so neither `pnpm install` nor `pnpm rebuild electron` would retry it (likely an incomplete postinstall during the original add, which logged registry EPERM retries). Fixed by running electron's `install.js` directly and adding an explicit `pnpm.onlyBuiltDependencies` allow-list to the root package.json for electron and esbuild — which also cleared a standing "Ignored build scripts: esbuild" warning. Verified: binary reports v43.2.0 and `pnpm --filter @spectrace/studio dev` now reaches "starting electron app". Recovery command documented in CLAUDE.md. My miss — I added Electron without ever checking its binary landed | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-APP vault extraction (decision by BP): all 20 REQ-APP requirements moved from specs/spectrace-product-spec.md into specs/requirements/, one file per requirement, matching the REQ-CORE/REQ-CLI treatment; product spec §4 rewritten as narrative with generated tables (v0.3.0), NFR-APP left inline for want of acceptance criteria; `spec:index` extended to the product spec. Vault grew 35 → 55 requirements, all validating clean. Status calls: REQ-APP-001 → `partial` (tree display holds, with a new test covering the stated 1,000-file/2 s bound; file operations and external-change detection do not), REQ-APP-002 stays `proposed` — Studio renders GFM but read-only in a separate pane, and AC2 asks for rendering *in live preview*, which the requirement defines as editable-in-place | Claude Code | draft; decision BP, pending BP review |
| 2026-08-02 | Studio walking skeleton (setup plan Phase 3; REQ-APP-001/002 partial): `apps/studio` scaffolded by hand rather than via `pnpm create @quick-start/electron`, which is interactive and would have dumped unreviewed files — electron-vite + React 19 + TS, with main/preload/renderer/shared layout and the typed `Api` IPC contract from setup plan §3.2. Vault reading (`src/main/vault.ts`) is deliberately Electron-free so it unit-tests in plain Node: recursive markdown tree, deterministic ordering, vendored/VCS directories excluded, POSIX paths, and path-escape rejection on reads. Renderer is the full walking skeleton — pick folder → tree → click file → markdown renders via react-markdown + remark-gfm — read-only, with contextIsolation on and a renderer CSP. 10 tests. Peer-dependency pinning needed: vite 7 (electron-vite 5 rejects 8), @vitejs/plugin-react 5, and TypeScript held at 5.6 to match the monorepo rather than the 7.x that resolved by default. CI gains ELECTRON_SKIP_BINARY_DOWNLOAD. REQ-APP status flips deferred pending the vault-extraction decision | Claude Code | draft; pending BP review |
| 2026-08-02 | Phase B gate check (unit 6, loop-driven): `spectrace init` scaffolded `.spectrace/` on this repository (6 files, exit 0), a second run changed nothing (exit 0), and `spectrace validate` reported 35 requirements with 0 violations and 0 warnings (exit 0). Frozen HOOK corpus re-validated at 12/12 clean with all 12 SHA-256 hashes intact. Full sweep green: `--frozen-lockfile`, build, typecheck, 177 tests, `spec:index:check`. Repo policy decided by BP: `.spectrace/config.yaml` and templates are committed (shared per-repo settings, so CI validates against the same config), generated index artifacts gitignored per REQ-CORE-012. Two of the three gate criteria are met; "Studio opens the vault read-only" remains outstanding because Studio Phase 3 was held — closing the gate is BP's call | Claude Code | draft; gate decision BP |
| 2026-08-02 | REQ-CLI-001 (Phase B unit 5, loop-driven): `spectrace init` scaffolds `.spectrace/config.yaml` plus a templates directory holding one file per REQ-CORE-003 template kind. Content comes from core — `renderDefaultConfig()` added to the config module, since REQ-CORE-004 owns the format and Studio's settings surfaces will want the same serialization — and the CLI only decides where bytes land. Idempotent by default, `--force` to overwrite, and `--force` touches only the files it manages. 9 CLI tests plus 2 core tests asserting the rendered default config round-trips to DEFAULT_CONFIG exactly, so an initialized repo behaves identically to a bare one. One test validates the scaffolded templates through the real CLI, closing REQ-CORE-003 AC1 end to end. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CLI-002 (Phase B unit 4, loop-driven): `spectrace validate` over the configured specification paths, wiring REQ-CORE-004's config loader to REQ-CORE-001/002's validator; documents re-rooted on their configured path so violation messages name a findable file; versioned `spectrace.validation-report` JSON envelope; exit 3 on violations, 0 clean, 1 on a missing repository. 9 tests including an explicit check that a broken document *outside* every configured path is not validated (AC1's "exactly"). Dogfooded: `pnpm cli validate --repo .` reports 35 requirements, 0 violations, exit 0. CLI spec §3 "currently shipping" note corrected from six stubs to five. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CORE-003 (Phase B unit 3, loop-driven): five specification templates (use case, functional, non-functional, architecturally significant, acceptance criteria) in `packages/core/src/templates/`, embedded as strings because tsc emits only JS and a `.md` under src/ would never reach dist/. `nextRequirementId` generates a collision-free ID from the IDs already in the vault, widening the zero-padding if the vault already uses wider numbers. 19 tests; each template is validated unedited through the REQ-CORE-001/002 validator, individually and as a set. Interpretation flagged for BP: the "acceptance criteria" template is rendered as a full requirement document rather than a paste-in fragment, since AC1 requires every template to instantiate with an ID and pass validation — the fragment reading contradicts its own AC. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CORE-004 (Phase B unit 2, loop-driven): YAML configuration loader in `packages/core/src/config/` — `.spectrace/config.yaml` with a `version` field covering spec paths, exclusions, retrieval mode and k, model/embedding settings, and confidence bands; every field resolves to a documented default so callers never branch on absence. Missing file, malformed YAML, unknown keys (reported by dotted path), and out-of-range values are all warnings, never failures. `toPlainValue` extracted to `src/internal/` and shared with the schema parser so both normalize at the YAML boundary. `RetrievalMode`/`ConfidenceBands` moved out of the index.ts scaffold into the config module. 17 tests including two source-greps asserting the engine reads no env vars and writes no console output, with a guard test so those greps cannot pass vacuously. Status flip: → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | REQ-CORE-001 + REQ-CORE-002 (Phase B unit 1, loop-driven): requirement schema parsing and set validation in `packages/core/src/schema/` — frontmatter parse with structuredClone-safe normalization, mandatory id/title/status, priority default P1, rationale from frontmatter or `## Rationale` body section, trace-link record shape, acceptance criteria from frontmatter or body, duplicate-ID detection naming every colliding file, all violations reported in one pass. 22 AC-mapped tests. Phase A stub types in `index.ts` (capitalized status vocabulary, mandatory rationale/priority) reconciled to the amended schema and the `vault` stub replaced. Dogfooded: this repo's 35 requirement files and the 12 frozen HOOK fixtures both validate with 0 violations and 0 warnings, all 12 fixture rationales resolving from body sections. Status flips: both → `implemented` | Claude Code | draft; pending BP review |
| 2026-08-02 | Single-source-of-truth fix (repo tooling, no REQ) — BP identified that the extracted narrative specs duplicated title/priority/status from the requirement frontmatter, i.e. two sources of truth for the same fields. Resolved by generation rather than deletion: `scripts/spec-index.mjs` rebuilds each marked table from `specs/requirements/` frontmatter, `pnpm spec:index:check` added to CI as the staleness guard. Also recorded: ground truth is frozen permanently (Phase B milestone row discharged, not pending); `partial` adopted on trial, revisit at the Phase B gate; validation walks `specs/requirements/` only — the narrative documents are prose, not requirement documents. Verified both directions: a hand-edited status fails the check (exit 1), and a new requirement file joins its section with no narrative edit | Claude Code | draft; problem spotted by BP, decisions BP |
| 2026-08-02 | CI health (SPEC-CLI-000 Phase A scaffold, no REQ): cross-OS typecheck failure diagnosed — `pnpm typecheck` ran before `pnpm build`, so `@spectrace/cli` could not resolve `@spectrace/core` (its `types` points at `dist/index.d.ts`, build-only); the three trailing TS18046/TS7006 errors were cascades of that one unresolved import, reproduced locally by moving `packages/core/dist` aside. Steps reordered build→typecheck (merged by BP as 0b7597f). Follow-up: GitHub Actions bumped to node24 runtimes (checkout v4→v7, pnpm/action-setup v4→v6, setup-node v4→v7), clearing the Node 20 deprecation warning; breaking changes checked as inapplicable and tags verified via git ls-remote | Claude Code | draft; pending BP review |
