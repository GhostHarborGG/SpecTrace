{
  "name": "todo-example",
  "version": "0.1.0",
  "private": true,
  "description": "A small to-do list library used as an example target repository for the spectrace-prelim tool.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  }
}
